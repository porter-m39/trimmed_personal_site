# Redirect index.html to root
Redirect 301 /index.html https://portermorgan.xyz/

# Force HTTPS and non-WWW (adjust if you prefer WWW)
RewriteEngine On
RewriteCond %{HTTPS} off [OR]
RewriteCond %{HTTP_HOST} ^www\.portermorgan\.xyz [NC]
RewriteRule ^(.*)$ https://portermorgan.xyz/$1 [L,R=301]